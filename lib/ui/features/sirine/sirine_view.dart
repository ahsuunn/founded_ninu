import 'package:flutter/material.dart';

class SirinePage extends StatelessWidget {
  const SirinePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Sirine Page")));
  }
}
